# My First Project

This is my first project pushed to GitHub as part of a class task.

## What's inside
- `index.html` — a simple login page built with HTML and CSS

## How it was made
- Created the file locally
- Initialized git with `git init`
- Connected to GitHub with `git remote add origin`
- Committed and pushed changes using `git add`, `git commit`, and `git push`